import store from './api/store'
import createStore from './api/createStore'
import StoreBase from './api/StoreBase'
import config from './api/config'
import ignore from './api/ignore'

export { config, store, ignore, createStore, StoreBase }

export default { config, store, ignore, createStore, StoreBase }
